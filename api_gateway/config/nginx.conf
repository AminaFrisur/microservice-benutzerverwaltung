 # Configuration specific to HTTP and affecting all virtual servers
 # load balance HTTP traffic to a group of servers,
 underscores_in_headers on;

    upstream rest-api-benutzerverwaltung {
            server rest-api-benutzerverwaltung1:8000;
            server rest-api-benutzerverwaltung2:8000;
            server rest-api-benutzerverwaltung3:8000;
            # gibt es anscheinend nur fuer nginxplus -> sticky cookie srv_id expires=1h domain=.example.com path=/;
            # Ebenfalls dynamsich neue instanzen von Microservices hinzuzufügen ist nur in nginxplus moeglich
        }


    server {
        location /getUsers {
            proxy_pass http://rest-api-benutzerverwaltung;
        }

         location /getUser/ {
            proxy_pass http://rest-api-benutzerverwaltung;
         }

         location /register {
              proxy_pass http://rest-api-benutzerverwaltung;
         }

         location /login {
            proxy_pass http://rest-api-benutzerverwaltung;
         }

         location /checkAuthUser {
            proxy_pass http://rest-api-benutzerverwaltung;
         }

         location /changeUserData {
             proxy_pass http://rest-api-benutzerverwaltung;
         }
    }
